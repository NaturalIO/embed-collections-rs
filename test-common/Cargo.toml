[package]
name = "embed-collections-test"
version = "0.1.0"
edition = "2024"

[dependencies]
captains-log = {version="0", features = ["ringfile"] }
log = { version="0"}
rstest = "0"

[features]
# enable log for dev
trace_log = []
