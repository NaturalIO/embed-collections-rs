[package]
name = "embed-seglist"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Cpu-cache friendly linkedlist with fixed-capacity segment"
keywords = ["linked-list"]
categories = ["data-structures", "no-std"]
homepage = "https://github.com/NaturalIO/embed-collections-rs"
readme = "../README.md"

[dependencies]
embed-collections = {path="../", version="1"}

[dev-dependencies]
embed-collections-test = {path="../test-common"}
log = { version="0"}


[features]
default = []

[lints]
workspace = true
